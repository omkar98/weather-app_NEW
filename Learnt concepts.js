A callback function is a function which is passed as an argument to another function, and this callback function executes when some of that function happens.

Example:
setTimeout(()=>{}, 2000);
()=>{} is callback function. It gets executed after the setTimeout() executes for 2000ms.
